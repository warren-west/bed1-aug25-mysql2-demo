const router = require('express').Router()


router.get('/login', (req, res) => {
    // Used to render the login page
})


router.post('/', (req, res) => {
    // Used to deal with the logic of the login request
    // Then redirect to a page
})

router.put()

router.delete()